<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:WorkoutApp.ViewModels"
             x:Class="WorkoutApp.Views.SettingsPage"
             x:DataType="viewModels:SettingsPageViewModel"
             Title="{Binding Title}">

    <!-- <Shell.SearchHandler> -->
    <!--     <controls:SettingsSearchHandler Placeholder="Search..." -->
    <!--                                     ShowsResults="true" -->
    <!--                                     SearchStrings="{Binding SearchStrings}" /> -->
    <!-- </Shell.SearchHandler> -->

    <!--<ContentPage.ToolbarItems>
        <ToolbarItem Text="Reset"
                     Order="Secondary"
                     Priority="0"
                     Command="{Binding ResetCommand}" />
    </ContentPage.ToolbarItems>-->

    <StackLayout>
        <Button Text="General" Command="{Binding GoToGeneralSettingsPageCommand}"
                FontSize="Medium" CornerRadius="0" />
        <Button Text="About" Command="{Binding GoToAboutPageCommand}"
                FontSize="Medium" CornerRadius="0" />
    </StackLayout>

</ContentPage>